export const Main = ({children}) => {
    return (
        <main id="main" className= "relative w-full h-fit min-h-[35rem] px-5 pt-8 pb-3 bg-slate-50/65">
            {children}
        </main>
    )
}